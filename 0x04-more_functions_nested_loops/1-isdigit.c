#include "main.h"
#include <stdio.h>

/**
  * _isdigit - check if character c is a digit
  *
  * @c: character
  * Return: 1 if digit, 0 otherwise
  */
 int _isdigit(int c)
 {
         return (c >= '0' && c <= '9');
 }
