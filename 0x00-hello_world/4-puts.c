#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
    char message[] = "Programming is like building a multilingual puzzle";
    fprintf(stdout, "%s\n", message);
    return (0);
}
